import {
  createRangeArray,
  hash2,
  hash3,
  hash4,
  iterableToArray,
  sortArray
} from "./chunk-H57AYTSJ.js";

// node_modules/molstar/lib/mol-data/int/tuple.js
var IntTuple;
(function(IntTuple2) {
  IntTuple2.Zero = 0;
  var _a = function() {
    var data = new ArrayBuffer(8);
    var data_1 = new ArrayBuffer(8);
    return {
      _int32: new Int32Array(data),
      _float64: new Float64Array(data),
      _int32_1: new Int32Array(data_1),
      _float64_1: new Float64Array(data_1)
    };
  }(), _int32 = _a._int32, _float64 = _a._float64, _int32_1 = _a._int32_1, _float64_1 = _a._float64_1;
  function is3(x) {
    return typeof x === "number";
  }
  IntTuple2.is = is3;
  function create2(fst2, snd2) {
    _int32[0] = fst2;
    _int32[1] = snd2;
    return _float64[0];
  }
  IntTuple2.create = create2;
  function diff(t) {
    _float64[0] = t;
    return _int32[1] - _int32[0];
  }
  IntTuple2.diff = diff;
  function fst(t) {
    _float64[0] = t;
    return _int32[0];
  }
  IntTuple2.fst = fst;
  function snd(t) {
    _float64[0] = t;
    return _int32[1];
  }
  IntTuple2.snd = snd;
  function areEqual4(a, b) {
    _float64[0] = a;
    _float64_1[0] = b;
    return _int32[0] === _int32_1[0] && _int32[1] === _int32_1[1];
  }
  IntTuple2.areEqual = areEqual4;
  function compare(a, b) {
    _float64[0] = a;
    _float64_1[0] = b;
    var x = _int32[0] - _int32_1[0];
    if (x !== 0)
      return x;
    return _int32[1] - _int32_1[1];
  }
  IntTuple2.compare = compare;
  function compareInArray(xs, i, j) {
    _float64[0] = xs[i];
    _float64_1[0] = xs[j];
    var x = _int32[0] - _int32_1[0];
    if (x !== 0)
      return x;
    return _int32[1] - _int32_1[1];
  }
  IntTuple2.compareInArray = compareInArray;
  function hashCode4(t) {
    _float64[0] = t;
    return hash2(_int32[0], _int32[1]);
  }
  IntTuple2.hashCode = hashCode4;
  function toString4(t) {
    _float64[0] = t;
    return "(".concat(_int32[0], ", ").concat(_int32[1], ")");
  }
  IntTuple2.toString = toString4;
})(IntTuple || (IntTuple = {}));

// node_modules/molstar/lib/mol-data/int/impl/interval.js
var Empty = IntTuple.Zero;
function ofRange(min4, max4) {
  return max4 < min4 ? IntTuple.create(min4, min4) : IntTuple.create(min4, max4 + 1);
}
function ofBounds(start4, end4) {
  return end4 <= start4 ? IntTuple.create(start4, start4) : IntTuple.create(start4, end4);
}
function ofLength(length) {
  return length < 0 ? IntTuple.create(0, 0) : IntTuple.create(0, length);
}
var is = IntTuple.is;
var start = IntTuple.fst;
var end = IntTuple.snd;
var min = IntTuple.fst;
function max(i) {
  return IntTuple.snd(i) - 1;
}
var size = IntTuple.diff;
var hashCode = IntTuple.hashCode;
var toString = IntTuple.toString;
function has(int, v) {
  return IntTuple.fst(int) <= v && v < IntTuple.snd(int);
}
function indexOf(int, x) {
  var m = start(int);
  return x >= m && x < end(int) ? x - m : -1;
}
function getAt(int, i) {
  return IntTuple.fst(int) + i;
}
var areEqual = IntTuple.areEqual;
function areIntersecting(a, b) {
  var sa = size(a), sb = size(b);
  if (sa === 0 && sb === 0)
    return true;
  return sa > 0 && sb > 0 && max(a) >= min(b) && min(a) <= max(b);
}
function isSubInterval(a, b) {
  if (!size(a))
    return size(b) === 0;
  if (!size(b))
    return true;
  return start(a) <= start(b) && end(a) >= end(b);
}
function findPredecessorIndex(int, v) {
  var s = start(int);
  if (v <= s)
    return 0;
  var e = end(int);
  if (v >= e)
    return e - s;
  return v - s;
}
function findPredecessorIndexInInterval(int, v, bounds) {
  var bS = start(bounds);
  var s = start(int);
  if (v <= bS + s)
    return bS;
  var bE = end(bounds);
  if (v >= bE + s)
    return bE;
  return v - s;
}
function findRange(int, min4, max4) {
  return ofBounds(findPredecessorIndex(int, min4), findPredecessorIndex(int, max4 + 1));
}
function intersect(a, b) {
  if (!areIntersecting(a, b))
    return Empty;
  return ofBounds(Math.max(start(a), start(b)), Math.min(end(a), end(b)));
}
function intersectionSize(a, b) {
  return size(findRange(a, min(b), max(b)));
}

// node_modules/molstar/lib/mol-data/int/interval.js
var Interval;
(function(Interval2) {
  Interval2.Empty = Empty;
  Interval2.ofSingleton = function(v) {
    return ofRange(v, v);
  };
  Interval2.ofRange = ofRange;
  Interval2.ofBounds = ofBounds;
  Interval2.ofLength = ofLength;
  Interval2.is = is;
  Interval2.has = has;
  Interval2.indexOf = indexOf;
  Interval2.getAt = getAt;
  Interval2.start = start;
  Interval2.end = end;
  Interval2.min = min;
  Interval2.max = max;
  Interval2.size = size;
  Interval2.hashCode = hashCode;
  Interval2.toString = toString;
  Interval2.areEqual = areEqual;
  Interval2.areIntersecting = areIntersecting;
  Interval2.isSubInterval = isSubInterval;
  Interval2.findPredecessorIndex = findPredecessorIndex;
  Interval2.findPredecessorIndexInInterval = findPredecessorIndexInInterval;
  Interval2.findRange = findRange;
  Interval2.intersectionSize = intersectionSize;
  Interval2.intersect = intersect;
})(Interval || (Interval = {}));

// node_modules/molstar/lib/mol-data/int/impl/sorted-array.js
var Empty2 = [];
function ofSingleton(v) {
  return [v];
}
function ofSortedArray(xs) {
  return xs;
}
function ofUnsortedArray(xs) {
  sortArray(xs);
  return xs;
}
function ofRange2(min4, max4) {
  if (max4 < min4)
    return [];
  var ret = new Int32Array(max4 - min4 + 1);
  for (var i = min4; i <= max4; i++)
    ret[i - min4] = i;
  return ret;
}
function is2(xs) {
  return xs && (Array.isArray(xs) || !!xs.buffer);
}
function isRange(xs) {
  return xs[xs.length - 1] - xs[0] + 1 === xs.length;
}
function start2(xs) {
  return xs[0];
}
function end2(xs) {
  return xs[xs.length - 1] + 1;
}
function min2(xs) {
  return xs[0];
}
function max2(xs) {
  return xs[xs.length - 1];
}
function size2(xs) {
  return xs.length;
}
function hashCode2(xs) {
  var s = xs.length;
  if (!s)
    return 0;
  if (s > 2)
    return hash4(s, xs[0], xs[s - 1], xs[s >> 1]);
  return hash3(s, xs[0], xs[s - 1]);
}
function toString2(xs) {
  var s = xs.length;
  if (s > 5)
    return "[".concat(xs[0], ", ").concat(xs[1], ", ..., ").concat(xs[s - 1], "], length ").concat(s);
  return "[".concat(xs.join(", "), "]");
}
function indexOf2(xs, v) {
  var l = xs.length;
  return l === 0 ? -1 : xs[0] <= v && v <= xs[l - 1] ? binarySearchRange(xs, v, 0, l) : -1;
}
function indexOfInInterval(xs, v, bounds) {
  return indexOfInRange(xs, v, Interval.start(bounds), Interval.end(bounds));
}
function indexOfInRange(xs, v, s, e) {
  var l = xs.length;
  return l === 0 || e <= s ? -1 : xs[s] <= v && v <= xs[e - 1] ? binarySearchRange(xs, v, s, e) : -1;
}
function has2(xs, v) {
  return indexOf2(xs, v) >= 0;
}
function areEqual2(a, b) {
  if (a === b)
    return true;
  var aSize = a.length;
  if (aSize !== b.length || a[0] !== b[0] || a[aSize - 1] !== b[aSize - 1])
    return false;
  if (isRange(a))
    return true;
  aSize--;
  for (var i = 1; i < aSize; i++) {
    if (a[i] !== b[i])
      return false;
  }
  return true;
}
function findPredecessorIndex2(xs, v) {
  var len = xs.length;
  if (v <= xs[0])
    return 0;
  if (v > xs[len - 1])
    return len;
  return binarySearchPredIndexRange(xs, v, 0, len);
}
function findPredecessorIndexInInterval2(xs, v, bounds) {
  var s = Interval.start(bounds), e = Interval.end(bounds);
  var sv = xs[s];
  if (v <= sv)
    return s;
  if (e > s && v > xs[e - 1])
    return e;
  if (v - sv <= 11)
    return linearSearchPredInRange(xs, v, s + 1, e);
  return binarySearchPredIndexRange(xs, v, s, e);
}
function findRange2(xs, min4, max4) {
  return Interval.ofBounds(findPredecessorIndex2(xs, min4), findPredecessorIndex2(xs, max4 + 1));
}
function binarySearchRange(xs, value, start4, end4) {
  var min4 = start4, max4 = end4 - 1;
  while (min4 <= max4) {
    if (min4 + 11 > max4) {
      for (var i = min4; i <= max4; i++) {
        if (value === xs[i])
          return i;
      }
      return -1;
    }
    var mid = min4 + max4 >> 1;
    var v = xs[mid];
    if (value < v)
      max4 = mid - 1;
    else if (value > v)
      min4 = mid + 1;
    else
      return mid;
  }
  return -1;
}
function binarySearchPredIndexRange(xs, value, start4, end4) {
  var min4 = start4, max4 = end4 - 1;
  while (min4 < max4) {
    if (min4 + 11 > max4) {
      for (var i = min4; i <= max4; i++) {
        if (value <= xs[i])
          return i;
      }
      return max4 + 1;
    }
    var mid = min4 + max4 >> 1;
    var v = xs[mid];
    if (value < v)
      max4 = mid - 1;
    else if (value > v)
      min4 = mid + 1;
    else
      return mid;
  }
  if (min4 > max4)
    return max4 + 1;
  return xs[min4] >= value ? min4 : min4 + 1;
}
function linearSearchPredInRange(xs, value, start4, end4) {
  for (var i = start4; i < end4; i++) {
    if (value <= xs[i])
      return i;
  }
  return end4;
}
function areIntersecting2(a, b) {
  if (a === b)
    return true;
  var _a = getSuitableIntersectionRange(a, b), i = _a.startI, j = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y)
      i++;
    else if (x > y)
      j++;
    else
      return true;
  }
  return false;
}
function isSubset(a, b) {
  if (a === b)
    return true;
  var lenB = b.length;
  var _a = getSuitableIntersectionRange(a, b), i = _a.startI, j = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  if (endJ - j < lenB || endI - i < lenB)
    return false;
  var equal = 0;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      i++;
    } else if (x > y) {
      j++;
    } else {
      i++;
      j++;
      equal++;
    }
  }
  return equal === lenB;
}
function union(a, b) {
  if (a === b)
    return a;
  var lenA = a.length, lenB = b.length;
  if (lenA === 0)
    return b;
  if (lenB === 0)
    return a;
  if (a[0] > b[0])
    return union(b, a);
  var _a = getSuitableIntersectionRange(a, b), startI = _a.startI, startJ = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  var commonCount = getCommonCount(a, b, startI, startJ, endI, endJ);
  if (commonCount === lenA && commonCount === lenB || commonCount === lenB)
    return a;
  if (commonCount === lenA)
    return b;
  var indices = new Int32Array(lenA + lenB - commonCount);
  var i = 0, j = 0, offset = 0;
  for (i = 0; i < startI; i++)
    indices[offset++] = a[i];
  while (j < endJ && a[startI] > b[j])
    indices[offset++] = b[j++];
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      indices[offset++] = x;
      i++;
    } else if (x > y) {
      indices[offset++] = y;
      j++;
    } else {
      indices[offset++] = x;
      i++;
      j++;
    }
  }
  for (; i < endI; i++)
    indices[offset++] = a[i];
  for (; j < endJ; j++)
    indices[offset++] = b[j];
  for (; i < lenA; i++)
    indices[offset++] = a[i];
  for (; j < lenB; j++)
    indices[offset++] = b[j];
  return ofSortedArray(indices);
}
function intersectionSize2(a, b) {
  if (a === b)
    return size2(a);
  var _a = getSuitableIntersectionRange(a, b), startI = _a.startI, startJ = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  return getCommonCount(a, b, startI, startJ, endI, endJ);
}
function getCommonCount(a, b, startI, startJ, endI, endJ) {
  var i = startI, j = startJ;
  var commonCount = 0;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      i++;
    } else if (x > y) {
      j++;
    } else {
      i++;
      j++;
      commonCount++;
    }
  }
  return commonCount;
}
function intersect2(a, b) {
  if (a === b)
    return a;
  var _a = getSuitableIntersectionRange(a, b), startI = _a.startI, startJ = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  var commonCount = getCommonCount(a, b, startI, startJ, endI, endJ);
  var lenA = a.length, lenB = b.length;
  if (!commonCount)
    return Empty2;
  if (commonCount === lenA && commonCount === lenB || commonCount === lenB)
    return b;
  if (commonCount === lenA)
    return a;
  var indices = new Int32Array(commonCount);
  var offset = 0;
  var i = startI;
  var j = startJ;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      i++;
    } else if (x > y) {
      j++;
    } else {
      indices[offset++] = x;
      i++;
      j++;
    }
  }
  return ofSortedArray(indices);
}
function subtract(a, b) {
  if (a === b)
    return Empty2;
  var lenA = a.length;
  var _a = getSuitableIntersectionRange(a, b), sI = _a.startI, sJ = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  var i = sI, j = sJ;
  var commonCount = 0;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      i++;
    } else if (x > y) {
      j++;
    } else {
      i++;
      j++;
      commonCount++;
    }
  }
  if (!commonCount)
    return a;
  if (commonCount >= lenA)
    return Empty2;
  var indices = new Int32Array(lenA - commonCount);
  var offset = 0;
  for (var k = 0; k < sI; k++)
    indices[offset++] = a[k];
  i = sI;
  j = sJ;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      indices[offset++] = x;
      i++;
    } else if (x > y) {
      j++;
    } else {
      i++;
      j++;
    }
  }
  for (; i < lenA; i++)
    indices[offset++] = a[i];
  return ofSortedArray(indices);
}
function deduplicate(xs) {
  if (xs.length < 2)
    return xs;
  var count2 = 1;
  for (var i = 0, _i = xs.length - 1; i < _i; i++) {
    if (xs[i] !== xs[i + 1])
      count2++;
  }
  if (count2 === xs.length)
    return xs;
  var ret = new Int32Array(count2);
  var o = 0;
  for (var i = 0, _i = xs.length - 1; i < _i; i++) {
    if (xs[i] !== xs[i + 1])
      ret[o++] = xs[i];
  }
  ret[o] = xs[xs.length - 1];
  return ret;
}
function indicesOf(a, b) {
  if (areEqual2(a, b))
    return ofSortedArray(createRangeArray(0, a.length - 1));
  var _a = getSuitableIntersectionRange(a, b), sI = _a.startI, sJ = _a.startJ, endI = _a.endI, endJ = _a.endJ;
  var i = sI, j = sJ;
  var commonCount = 0;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      i++;
    } else if (x > y) {
      j++;
    } else {
      i++;
      j++;
      commonCount++;
    }
  }
  var lenA = a.length;
  if (!commonCount)
    return Empty2;
  if (commonCount === lenA)
    return ofSortedArray(createRangeArray(0, a.length - 1));
  var indices = new Int32Array(commonCount);
  var offset = 0;
  i = sI;
  j = sJ;
  while (i < endI && j < endJ) {
    var x = a[i], y = b[j];
    if (x < y) {
      i++;
    } else if (x > y) {
      j++;
    } else {
      indices[offset++] = i;
      i++;
      j++;
    }
  }
  return ofSortedArray(indices);
}
var _maxIntRangeRet = { startI: 0, startJ: 0, endI: 0, endJ: 0 };
function getSuitableIntersectionRange(a, b) {
  var la = a.length, lb = b.length;
  var ratio = la / lb;
  if (la >= 128 || lb >= 128 || ratio <= 0.34 || ratio >= 2.99) {
    _maxIntRangeRet.startI = findPredecessorIndex2(a, start2(b));
    _maxIntRangeRet.startJ = findPredecessorIndex2(b, start2(a));
    _maxIntRangeRet.endI = findPredecessorIndex2(a, end2(b));
    _maxIntRangeRet.endJ = findPredecessorIndex2(b, end2(a));
  } else {
    _maxIntRangeRet.startI = 0;
    _maxIntRangeRet.startJ = 0;
    _maxIntRangeRet.endI = la;
    _maxIntRangeRet.endJ = lb;
  }
  return _maxIntRangeRet;
}

// node_modules/molstar/lib/mol-data/int/sorted-array.js
var SortedArray;
(function(SortedArray2) {
  SortedArray2.Empty = Empty2;
  SortedArray2.ofUnsortedArray = ofUnsortedArray;
  SortedArray2.ofSingleton = ofSingleton;
  SortedArray2.ofSortedArray = ofSortedArray;
  SortedArray2.ofRange = ofRange2;
  SortedArray2.ofBounds = function(min4, max4) {
    return ofRange2(min4, max4 - 1);
  };
  SortedArray2.is = is2;
  SortedArray2.isRange = isRange;
  SortedArray2.has = has2;
  SortedArray2.indexOf = indexOf2;
  SortedArray2.indexOfInInterval = indexOfInInterval;
  SortedArray2.indexOfInRange = indexOfInRange;
  SortedArray2.start = start2;
  SortedArray2.end = end2;
  SortedArray2.min = min2;
  SortedArray2.max = max2;
  SortedArray2.size = size2;
  SortedArray2.hashCode = hashCode2;
  SortedArray2.toString = toString2;
  SortedArray2.areEqual = areEqual2;
  SortedArray2.areIntersecting = areIntersecting2;
  SortedArray2.isSubset = isSubset;
  SortedArray2.union = union;
  SortedArray2.intersect = intersect2;
  SortedArray2.subtract = subtract;
  SortedArray2.findPredecessorIndex = findPredecessorIndex2;
  SortedArray2.findPredecessorIndexInInterval = findPredecessorIndexInInterval2;
  SortedArray2.findRange = findRange2;
  SortedArray2.intersectionSize = intersectionSize2;
  SortedArray2.deduplicate = deduplicate;
  SortedArray2.indicesOf = indicesOf;
})(SortedArray || (SortedArray = {}));

// node_modules/molstar/lib/mol-data/int/impl/ordered-set.js
var Empty3 = Interval.Empty;
var ofSingleton2 = Interval.ofSingleton;
var ofRange3 = Interval.ofRange;
var ofBounds2 = Interval.ofBounds;
function ofSortedArray2(xs) {
  if (!xs.length)
    return Empty3;
  if (SortedArray.isRange(xs))
    return Interval.ofRange(xs[0], xs[xs.length - 1]);
  return xs;
}
function size3(set) {
  return Interval.is(set) ? Interval.size(set) : SortedArray.size(set);
}
function has3(set, x) {
  return Interval.is(set) ? Interval.has(set, x) : SortedArray.has(set, x);
}
function indexOf3(set, x) {
  return Interval.is(set) ? Interval.indexOf(set, x) : SortedArray.indexOf(set, x);
}
function getAt2(set, i) {
  return Interval.is(set) ? Interval.getAt(set, i) : set[i];
}
function min3(set) {
  return Interval.is(set) ? Interval.min(set) : SortedArray.min(set);
}
function max3(set) {
  return Interval.is(set) ? Interval.max(set) : SortedArray.max(set);
}
function start3(set) {
  return Interval.is(set) ? Interval.start(set) : SortedArray.start(set);
}
function end3(set) {
  return Interval.is(set) ? Interval.end(set) : SortedArray.end(set);
}
function hashCode3(set) {
  return Interval.is(set) ? Interval.hashCode(set) : SortedArray.hashCode(set);
}
function toString3(set) {
  return Interval.is(set) ? Interval.toString(set) : SortedArray.toString(set);
}
function areEqual3(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return Interval.areEqual(a, b);
    return areEqualIS(a, b);
  } else if (Interval.is(b))
    return areEqualIS(b, a);
  return SortedArray.areEqual(a, b);
}
function areIntersecting3(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return Interval.areIntersecting(a, b);
    return areIntersectingSI(b, a);
  } else if (Interval.is(b))
    return areIntersectingSI(a, b);
  return SortedArray.areIntersecting(a, b);
}
function isSubset2(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return Interval.isSubInterval(a, b);
    return isSubsetIS(a, b);
  } else if (Interval.is(b))
    return isSubsetSI(a, b);
  return SortedArray.isSubset(a, b);
}
function findPredecessorIndex3(set, x) {
  return Interval.is(set) ? Interval.findPredecessorIndex(set, x) : SortedArray.findPredecessorIndex(set, x);
}
function findPredecessorIndexInInterval3(set, x, bounds) {
  return Interval.is(set) ? Interval.findPredecessorIndexInInterval(set, x, bounds) : SortedArray.findPredecessorIndexInInterval(set, x, bounds);
}
function findRange3(set, min4, max4) {
  return Interval.is(set) ? Interval.findRange(set, min4, max4) : SortedArray.findRange(set, min4, max4);
}
function intersectionSize3(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return Interval.intersectionSize(a, b);
    return intersectionSizeSI(b, a);
  } else if (Interval.is(b))
    return intersectionSizeSI(a, b);
  return SortedArray.intersectionSize(a, b);
}
function union2(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return unionII(a, b);
    return unionSI(b, a);
  } else if (Interval.is(b))
    return unionSI(a, b);
  return ofSortedArray2(SortedArray.union(a, b));
}
function intersect3(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return Interval.intersect(a, b);
    return intersectSI(b, a);
  } else if (Interval.is(b))
    return intersectSI(a, b);
  return ofSortedArray2(SortedArray.intersect(a, b));
}
function subtract2(a, b) {
  if (Interval.is(a)) {
    if (Interval.is(b))
      return subtractII(a, b);
    return subtractIS(a, b);
  } else if (Interval.is(b))
    return subtractSI(a, b);
  return ofSortedArray2(SortedArray.subtract(a, b));
}
function areEqualIS(a, b) {
  return Interval.size(a) === SortedArray.size(b) && Interval.start(a) === SortedArray.start(b) && Interval.end(a) === SortedArray.end(b);
}
function areIntersectingSI(a, b) {
  return a.length !== 0 && Interval.size(SortedArray.findRange(a, Interval.min(b), Interval.max(b))) !== 0;
}
function isSubsetSI(a, b) {
  var minB = Interval.min(b), maxB = Interval.max(b);
  if (maxB - minB + 1 === 0)
    return true;
  var minA = SortedArray.min(a), maxA = SortedArray.max(a);
  if (minB < minA || maxB > maxA)
    return false;
  var r = SortedArray.findRange(a, minB, maxB);
  return Interval.size(r) === Interval.size(b);
}
function isSubsetIS(a, b) {
  var minA = Interval.min(a), maxA = Interval.max(a);
  if (maxA - minA + 1 === 0)
    return false;
  var minB = SortedArray.min(b), maxB = SortedArray.max(b);
  return minB >= minA && maxB <= maxA;
}
function areRangesIntersecting(a, b) {
  var sa = size3(a), sb = size3(b);
  if (sa === 0 && sb === 0)
    return true;
  return sa > 0 && sb > 0 && max3(a) >= min3(b) && min3(a) <= max3(b);
}
function isRangeSubset(a, b) {
  if (!size3(a))
    return size3(b) === 0;
  if (!size3(b))
    return true;
  return min3(a) <= min3(b) && max3(a) >= max3(b);
}
function unionII(a, b) {
  if (Interval.areEqual(a, b))
    return a;
  var sizeA = Interval.size(a), sizeB = Interval.size(b);
  if (!sizeB)
    return a;
  if (!sizeA)
    return b;
  var minA = Interval.min(a), minB = Interval.min(b);
  if (areRangesIntersecting(a, b))
    return Interval.ofRange(Math.min(minA, minB), Math.max(Interval.max(a), Interval.max(b)));
  var lSize, lMin, rSize, rMin;
  if (minA < minB) {
    lSize = sizeA;
    lMin = minA;
    rSize = sizeB;
    rMin = minB;
  } else {
    lSize = sizeB;
    lMin = minB;
    rSize = sizeA;
    rMin = minA;
  }
  var arr = new Int32Array(sizeA + sizeB);
  for (var i = 0; i < lSize; i++)
    arr[i] = i + lMin;
  for (var i = 0; i < rSize; i++)
    arr[i + lSize] = i + rMin;
  return ofSortedArray2(arr);
}
function unionSI(a, b) {
  var bSize = Interval.size(b);
  if (!bSize)
    return a;
  if (isRangeSubset(b, a))
    return b;
  var min4 = Interval.min(b), max4 = Interval.max(b);
  var r = SortedArray.findRange(a, min4, max4);
  var start4 = Interval.start(r), end4 = Interval.end(r);
  var indices = new Int32Array(start4 + (a.length - end4) + bSize);
  var offset = 0;
  for (var i = 0; i < start4; i++)
    indices[offset++] = a[i];
  for (var i = min4; i <= max4; i++)
    indices[offset++] = i;
  for (var i = end4, _i = a.length; i < _i; i++)
    indices[offset++] = a[i];
  return ofSortedArray2(indices);
}
function intersectionSizeSI(a, b) {
  if (!Interval.size(b))
    return 0;
  var r = SortedArray.findRange(a, Interval.min(b), Interval.max(b));
  return Interval.end(r) - Interval.start(r);
}
function intersectSI(a, b) {
  if (!Interval.size(b))
    return Empty3;
  var r = SortedArray.findRange(a, Interval.min(b), Interval.max(b));
  var start4 = Interval.start(r), end4 = Interval.end(r);
  var resultSize = end4 - start4;
  if (!resultSize)
    return Empty3;
  if (resultSize === a.length)
    return a;
  var indices = new Int32Array(resultSize);
  var offset = 0;
  for (var i = start4; i < end4; i++) {
    indices[offset++] = a[i];
  }
  return ofSortedArray2(indices);
}
function subtractII(a, b) {
  if (Interval.areEqual(a, b))
    return Empty3;
  if (!Interval.areIntersecting(a, b))
    return a;
  var minA = Interval.min(a), maxA = Interval.max(a);
  var minB = Interval.min(b), maxB = Interval.max(b);
  if (maxA < minA || maxB < minB)
    return a;
  if (Interval.isSubInterval(b, a))
    return Empty3;
  if (Interval.isSubInterval(a, b)) {
    var l = minB - minA, r = maxA - maxB;
    if (l <= 0)
      return Interval.ofRange(maxB + 1, maxB + r);
    if (r <= 0)
      return Interval.ofRange(minA, minA + l - 1);
    var ret = new Int32Array(l + r);
    var offset = 0;
    for (var i = 0; i < l; i++)
      ret[offset++] = minA + i;
    for (var i = 1; i <= r; i++)
      ret[offset++] = maxB + i;
    return ofSortedArray2(ret);
  }
  if (minA < minB)
    return Interval.ofRange(minA, minB - 1);
  return Interval.ofRange(maxB + 1, maxA);
}
function subtractSI(a, b) {
  var min4 = Interval.min(b), max4 = Interval.max(b);
  if (max4 < min4)
    return a;
  var r = SortedArray.findRange(a, min4, max4);
  var start4 = Interval.start(r), end4 = Interval.end(r);
  var resultSize = a.length - (end4 - start4);
  if (resultSize <= 0)
    return Empty3;
  if (resultSize === a.length)
    return a;
  var ret = new Int32Array(resultSize);
  var offset = 0;
  for (var i = 0; i < start4; i++)
    ret[offset++] = a[i];
  for (var i = end4, _i = a.length; i < _i; i++)
    ret[offset++] = a[i];
  return ofSortedArray2(ret);
}
function subtractIS(a, b) {
  var min4 = Interval.min(a), max4 = Interval.max(a);
  if (max4 < min4)
    return a;
  var rSize = max4 - min4 + 1;
  var interval = SortedArray.findRange(b, min4, max4);
  var start4 = Interval.start(interval), end4 = Interval.end(interval);
  var commonCount = end4 - start4;
  if (commonCount === 0)
    return a;
  var resultSize = rSize - commonCount;
  if (resultSize <= 0)
    return Empty3;
  var ret = new Int32Array(resultSize);
  var li = b.length - 1;
  var fst = b[Math.min(start4, li)], last = b[Math.min(end4, li)];
  var offset = 0;
  for (var i = min4; i < fst; i++)
    ret[offset++] = i;
  for (var i = fst; i <= last; i++) {
    if (SortedArray.indexOfInInterval(b, i, interval) < 0)
      ret[offset++] = i;
  }
  for (var i = last + 1; i <= max4; i++)
    ret[offset++] = i;
  return ofSortedArray2(ret);
}
function forEach(set, f, ctx) {
  if (Interval.is(set)) {
    var start_1 = Interval.min(set);
    for (var i = start_1, _i = Interval.max(set); i <= _i; i++) {
      f(i, i - start_1, ctx);
    }
  } else {
    for (var i = 0, _i = set.length; i < _i; i++) {
      f(set[i], i, ctx);
    }
  }
  return ctx;
}
function forEachSegment(set, segment, f, ctx) {
  if (Interval.is(set)) {
    var sI = 0;
    for (var i = Interval.min(set), _i = Interval.max(set); i <= _i; i++) {
      var s = segment(i);
      var endI = i + 1;
      while (endI < _i && segment(endI) === s)
        endI++;
      i = endI - 1;
      f(s, sI, ctx);
      sI++;
    }
  } else {
    var sI = 0;
    for (var i = 0, _i = set.length; i < _i; i++) {
      var s = segment(set[i]);
      var endI = i + 1;
      while (endI < _i && segment(set[endI]) === s)
        endI++;
      i = endI - 1;
      f(s, sI, ctx);
      sI++;
    }
  }
  return ctx;
}
function indexedIntersect(idxA, a, b) {
  if (a === b)
    return idxA;
  var lenI = size3(idxA), lenA = a.length, lenB = b.length;
  if (lenI === 0 || lenA === 0 || lenB === 0)
    return Empty3;
  var startJ = SortedArray.findPredecessorIndex(b, a[min3(idxA)]);
  var endJ = SortedArray.findPredecessorIndex(b, a[max3(idxA)] + 1);
  var commonCount = 0;
  var offset = 0;
  var O = 0;
  var j = startJ;
  while (O < lenI && j < endJ) {
    var x = a[getAt2(idxA, O)], y = b[j];
    if (x < y) {
      O++;
    } else if (x > y) {
      j++;
    } else {
      commonCount++;
      O++;
      j++;
    }
  }
  if (commonCount === 0)
    return Empty3;
  if (commonCount === lenA && commonCount === lenB)
    return idxA;
  var indices = new Int32Array(commonCount);
  offset = 0;
  O = 0;
  j = startJ;
  while (O < lenI && j < endJ) {
    var x = a[getAt2(idxA, O)], y = b[j];
    if (x < y) {
      O++;
    } else if (x > y) {
      j++;
    } else {
      indices[offset++] = j;
      O++;
      j++;
    }
  }
  return ofSortedArray2(indices);
}

// node_modules/molstar/lib/mol-data/int/ordered-set.js
var OrderedSet;
(function(OrderedSet2) {
  OrderedSet2.Empty = Empty3;
  OrderedSet2.ofSingleton = ofSingleton2;
  OrderedSet2.ofRange = ofRange3;
  OrderedSet2.ofBounds = ofBounds2;
  OrderedSet2.ofSortedArray = ofSortedArray2;
  OrderedSet2.has = has3;
  OrderedSet2.indexOf = indexOf3;
  OrderedSet2.getAt = getAt2;
  OrderedSet2.min = min3;
  OrderedSet2.max = max3;
  OrderedSet2.start = start3;
  OrderedSet2.end = end3;
  OrderedSet2.size = size3;
  OrderedSet2.hashCode = hashCode3;
  OrderedSet2.areEqual = areEqual3;
  OrderedSet2.areIntersecting = areIntersecting3;
  OrderedSet2.isSubset = isSubset2;
  OrderedSet2.union = union2;
  OrderedSet2.intersect = intersect3;
  OrderedSet2.indexedIntersect = indexedIntersect;
  OrderedSet2.subtract = subtract2;
  OrderedSet2.findPredecessorIndex = findPredecessorIndex3;
  OrderedSet2.findPredecessorIndexInInterval = findPredecessorIndexInInterval3;
  OrderedSet2.findRange = findRange3;
  OrderedSet2.intersectionSize = intersectionSize3;
  function forEach2(set, f, ctx) {
    return forEach(set, f, ctx);
  }
  OrderedSet2.forEach = forEach2;
  function forEachSegment2(set, segment, f, ctx) {
    return forEachSegment(set, segment, f, ctx);
  }
  OrderedSet2.forEachSegment = forEachSegment2;
  function isInterval(set) {
    return Interval.is(set);
  }
  OrderedSet2.isInterval = isInterval;
  function isSortedArray(set) {
    return !Interval.is(set);
  }
  OrderedSet2.isSortedArray = isSortedArray;
  function toArray(set) {
    var array = [];
    OrderedSet2.forEach(set, function(v) {
      return array.push(v);
    });
    return array;
  }
  OrderedSet2.toArray = toArray;
  function toString4(set) {
    return toString3(set);
  }
  OrderedSet2.toString = toString4;
})(OrderedSet || (OrderedSet = {}));

// node_modules/molstar/lib/mol-data/int/impl/segmentation.js
function create(values) {
  var offsets = SortedArray.ofSortedArray(values);
  var max4 = SortedArray.max(offsets);
  var index = new Int32Array(max4);
  for (var i = 0, _i = values.length - 1; i < _i; i++) {
    for (var j = values[i], _j = values[i + 1]; j < _j; j++) {
      index[j] = i;
    }
  }
  return { offsets, index, count: values.length - 1 };
}
function ofOffsets(offsets, bounds) {
  var s = Interval.start(bounds);
  var segments2 = new Int32Array(offsets.length + 1);
  for (var i = 0, _i = offsets.length; i < _i; i++) {
    segments2[i] = offsets[i] - s;
  }
  segments2[offsets.length] = Interval.end(bounds) - s;
  return create(segments2);
}
function count(_a) {
  var count2 = _a.count;
  return count2;
}
function getSegment(_a, value) {
  var index = _a.index;
  return index[value];
}
function projectValue(_a, set, value) {
  var offsets = _a.offsets;
  var last = OrderedSet.max(offsets);
  var idx = value >= last ? -1 : OrderedSet.findPredecessorIndex(offsets, value - 1);
  return OrderedSet.findRange(set, OrderedSet.getAt(offsets, idx), OrderedSet.getAt(offsets, idx + 1) - 1);
}
var SegmentIterator = (
  /** @class */
  function() {
    function SegmentIterator2(segments2, segmentMap, set, inputRange) {
      this.segments = segments2;
      this.segmentMap = segmentMap;
      this.set = set;
      this.segmentMin = 0;
      this.segmentMax = 0;
      this.setRange = Interval.Empty;
      this.value = { index: 0, start: 0, end: 0 };
      this.hasNext = false;
      this.setRange = inputRange;
      this.updateSegmentRange();
    }
    SegmentIterator2.prototype.move = function() {
      while (this.hasNext) {
        if (this.updateValue()) {
          this.value.index = this.segmentMin++;
          this.hasNext = this.segmentMax >= this.segmentMin && Interval.size(this.setRange) > 0;
          break;
        } else {
          this.updateSegmentRange();
        }
      }
      return this.value;
    };
    SegmentIterator2.prototype.updateValue = function() {
      var segmentEnd = this.segments[this.segmentMin + 1];
      var setEnd = OrderedSet.findPredecessorIndexInInterval(this.set, segmentEnd, this.setRange);
      this.value.start = Interval.start(this.setRange);
      this.value.end = setEnd;
      this.setRange = Interval.ofBounds(setEnd, Interval.end(this.setRange));
      return setEnd > this.value.start;
    };
    SegmentIterator2.prototype.updateSegmentRange = function() {
      var sMin = Interval.min(this.setRange), sMax = Interval.max(this.setRange);
      if (sMax < sMin) {
        this.hasNext = false;
        return;
      }
      this.segmentMin = this.segmentMap[OrderedSet.getAt(this.set, sMin)];
      this.segmentMax = this.segmentMap[OrderedSet.getAt(this.set, sMax)];
      this.hasNext = this.segmentMax >= this.segmentMin;
    };
    SegmentIterator2.prototype.setSegment = function(segment) {
      this.setRange = Interval.ofBounds(segment.start, segment.end);
      this.updateSegmentRange();
    };
    return SegmentIterator2;
  }()
);
function segments(segs, set, segment) {
  var int = typeof segment !== "undefined" ? Interval.ofBounds(segment.start, segment.end) : Interval.ofBounds(0, OrderedSet.size(set));
  return new SegmentIterator(segs.offsets, segs.index, set, int);
}

// node_modules/molstar/lib/mol-data/int/segmentation.js
var Segmentation;
(function(Segmentation2) {
  Segmentation2.create = create;
  Segmentation2.ofOffsets = ofOffsets;
  Segmentation2.count = count;
  Segmentation2.getSegment = getSegment;
  Segmentation2.projectValue = projectValue;
  Segmentation2.transientSegments = segments;
})(Segmentation || (Segmentation = {}));

// node_modules/molstar/lib/mol-data/int/linked-index.js
function LinkedIndex(size4) {
  return new LinkedIndexImpl(size4);
}
var LinkedIndexImpl = (
  /** @class */
  function() {
    function LinkedIndexImpl2(size4) {
      this.head = size4 > 0 ? 0 : -1;
      this.prev = new Int32Array(size4);
      this.next = new Int32Array(size4);
      for (var i = 0; i < size4; i++) {
        this.next[i] = i + 1;
        this.prev[i] = i - 1;
      }
      this.prev[0] = -1;
      this.next[size4 - 1] = -1;
    }
    LinkedIndexImpl2.prototype.remove = function(i) {
      var _a = this, prev = _a.prev, next = _a.next;
      var p = prev[i], n = next[i];
      if (p >= 0) {
        next[p] = n;
        prev[i] = -1;
      }
      if (n >= 0) {
        prev[n] = p;
        next[i] = -1;
      }
      if (i === this.head) {
        if (p < 0)
          this.head = n;
        else
          this.head = p;
      }
    };
    LinkedIndexImpl2.prototype.has = function(i) {
      return this.prev[i] >= 0 || this.next[i] >= 0 || this.head === i;
    };
    return LinkedIndexImpl2;
  }()
);

// node_modules/molstar/lib/mol-data/int/map.js
var IntMap;
(function(IntMap2) {
  IntMap2.Empty = /* @__PURE__ */ new Map();
  function keyArray(map) {
    return iterableToArray(map.keys());
  }
  IntMap2.keyArray = keyArray;
  function Mutable() {
    return /* @__PURE__ */ new Map();
  }
  IntMap2.Mutable = Mutable;
  function asImmutable(map) {
    return map;
  }
  IntMap2.asImmutable = asImmutable;
  function copy(map) {
    var ret = Mutable();
    var it = map.keys();
    while (true) {
      var _a = it.next(), done = _a.done, value = _a.value;
      if (done)
        break;
      ret.set(value, map.get(value));
    }
    return ret;
  }
  IntMap2.copy = copy;
  function addFrom(map, src) {
    var it = src.keys();
    while (true) {
      var _a = it.next(), done = _a.done, value = _a.value;
      if (done)
        break;
      map.set(value, src.get(value));
    }
    return map;
  }
  IntMap2.addFrom = addFrom;
})(IntMap || (IntMap = {}));

// node_modules/molstar/lib/mol-data/iterator.js
var ArrayIteratorImpl = (
  /** @class */
  function() {
    function ArrayIteratorImpl2(xs) {
      this.xs = [];
      this.index = -1;
      this.length = 0;
      this.hasNext = false;
      this.length = xs.length;
      this.hasNext = xs.length > 0;
      this.xs = xs;
      this.index = -1;
      this.lastValue = xs.length > 0 ? xs[0] : void 0;
    }
    ArrayIteratorImpl2.prototype.move = function() {
      ++this.index;
      this.lastValue = this.xs[this.index];
      this.hasNext = this.index < this.length - 1;
      return this.lastValue;
    };
    return ArrayIteratorImpl2;
  }()
);
var RangeIteratorImpl = (
  /** @class */
  function() {
    function RangeIteratorImpl2(min4, max4) {
      this.max = max4;
      this.value = 0;
      this.hasNext = false;
      this.value = min4 - 1;
      this.hasNext = max4 >= min4;
    }
    RangeIteratorImpl2.prototype.move = function() {
      ++this.value;
      this.hasNext = this.value < this.max;
      return this.value;
    };
    return RangeIteratorImpl2;
  }()
);
var ValueIterator = (
  /** @class */
  function() {
    function ValueIterator2(value) {
      this.value = value;
      this.hasNext = true;
    }
    ValueIterator2.prototype.move = function() {
      this.hasNext = false;
      return this.value;
    };
    return ValueIterator2;
  }()
);
var MapIteratorImpl = (
  /** @class */
  function() {
    function MapIteratorImpl2(base, f) {
      this.base = base;
      this.f = f;
      this.hasNext = false;
      this.hasNext = base.hasNext;
    }
    MapIteratorImpl2.prototype.move = function() {
      var v = this.f(this.base.move());
      this.hasNext = this.base.hasNext;
      return v;
    };
    return MapIteratorImpl2;
  }()
);
var FilterIteratorImpl = (
  /** @class */
  function() {
    function FilterIteratorImpl2(base, p) {
      this.base = base;
      this.p = p;
      this.hasNext = this.findNext();
    }
    FilterIteratorImpl2.prototype.move = function() {
      var ret = this.next;
      this.hasNext = this.findNext();
      return ret;
    };
    FilterIteratorImpl2.prototype.findNext = function() {
      while (this.base.hasNext) {
        this.next = this.base.move();
        if (this.p(this.next))
          return true;
      }
      return false;
    };
    return FilterIteratorImpl2;
  }()
);
var Iterator;
(function(Iterator2) {
  Iterator2.Empty = new RangeIteratorImpl(0, -1);
  function Array2(xs) {
    return new ArrayIteratorImpl(xs);
  }
  Iterator2.Array = Array2;
  function Value(value) {
    return new ValueIterator(value);
  }
  Iterator2.Value = Value;
  function Range(min4, max4) {
    return new RangeIteratorImpl(min4, max4);
  }
  Iterator2.Range = Range;
  function map(base, f) {
    return new MapIteratorImpl(base, f);
  }
  Iterator2.map = map;
  function filter(base, p) {
    return new FilterIteratorImpl(base, p);
  }
  Iterator2.filter = filter;
  function forEach2(it, f, ctx) {
    while (it.hasNext) {
      var c = f(it.move(), ctx);
      if (c)
        return ctx;
    }
    return ctx;
  }
  Iterator2.forEach = forEach2;
})(Iterator || (Iterator = {}));

export {
  IntTuple,
  Interval,
  SortedArray,
  OrderedSet,
  Segmentation,
  LinkedIndex,
  IntMap,
  Iterator
};
//# sourceMappingURL=chunk-655NZX6P.js.map
